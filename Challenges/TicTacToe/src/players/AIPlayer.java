package players;

//import Players.Move;

import gameController.Move;

public class AIPlayer extends Player {
    @Override
    public Move getMove() {
        return null;
    }


/*    public Players.AIPlayer (Player opponent) {
        this.opponent = opponent;
    }

    // AIStrategy strategy = new MinMaxController();

    private  void setStrategy(AIStrategy strategy) {
        this.strategy = strategy;
    }

    public Players.Move getMove() {
        strategy.getBestMove(g, true, this.getSymbol(), opponent.getSymbol(), 0, 1, -1);
        return startegy.getBestMove();
    }*/


}

